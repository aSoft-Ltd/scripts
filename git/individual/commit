#!/bin/bash

doAction() {
  msg=$1
  echo "----------------------------------------------------"
  git add .
  echo "Commiting: $msg"
	git commit -m "$msg" && printf "[\xE2\x9C\x94] Commited\n" || echo '[-] Skipping: Nothing to commit'
	printf "\n\n"
}

doAction "$1";